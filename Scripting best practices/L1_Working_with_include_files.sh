#!/bin/bash
#
# All about using incude files
#


echo 'you can use source or a fullstop like this ->> .

You would have a line like the one below to import sripts

. /etc/init.d/functions

where the functions are imported

Indlude files do nnot need to startt wiith a shebang'

echo '
INTERNAL and EXTERNAL COMMANDS

Internal Command: is loaded with the bash shell itself. More efficient as they are already in Memory.

External Command: Living as a file on the hard disk that is running the script. 
'
